"""Re-exported from hydrokg.data.synthetic so both the package (CLI --demo path) and
the test suite generate data via the exact same function."""

from hydrokg.data.synthetic import make_synthetic_basin

__all__ = ["make_synthetic_basin"]
