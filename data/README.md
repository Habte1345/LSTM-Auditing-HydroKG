# data/

Place (or symlink) your CAMELS_US dataset here, e.g.:

```
data/CAMELS_US/basin_mean_forcing/nldas_extended/...
```

Then pass `--camels_root data/CAMELS_US` to either CLI. This directory is gitignored --
CAMELS is tens of GB and never belongs in version control.
